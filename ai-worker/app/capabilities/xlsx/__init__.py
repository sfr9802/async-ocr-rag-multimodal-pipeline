"""XLSX extraction capability.

This package stays intentionally narrow: it reads .xlsx/.xlsm files with
openpyxl, emits structured JSON/Markdown artifacts, and leaves indexing to the
existing SearchUnit pipeline in core-api.
"""
