# Development-only dependencies.
#
# The test suite (pytest) is declared in pyproject.toml under
# [project.optional-dependencies].dev; this file adds the tooling
# needed by the Optuna tuning loop (scripts.tune / scripts.summarize_study).
#
# Install alongside the runtime requirements:
#   pip install -r requirements.txt -r requirements-dev.txt
#
# PyYAML is a transitive dependency of several packages already in
# requirements.txt, but is listed explicitly here because tune.py uses
# it directly to parse active.yaml.

optuna==4.5.0
optuna-dashboard>=0.15,<1.0
matplotlib>=3.9,<4
plotly>=5.22,<7      # optional; imported by optuna-dashboard for interactive views
PyYAML>=6,<7
