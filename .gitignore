# ---- OS / editors ----
.DS_Store
Thumbs.db
.idea/
.vscode/
*.iml
*.swp

# ---- Frontend Vite/React source (bundle output is committed) ----
# frontend/index.html is the bundled artifact (committed). The Vite source
# project under frontend/app/ generates it via the web-artifacts-builder skill;
# its node_modules + build caches do not belong in git.
frontend/app/node_modules/
frontend/app/dist/
frontend/app/.parcel-cache/
frontend/app/bundle.html

# ---- Claude Code local state ----
# The whole .claude/ directory is per-user (settings, launch config,
# scheduled-task lockfiles, session logs, slash commands).
.claude/

# ---- Java / Maven ----
core-api/target/
core-api/.mvn/wrapper/maven-wrapper.jar
core-api/build/
core-api/out/
core-api/**/*.class
core-api/logs/
core-api/HELP.md

# ---- Python ----
ai-worker/.venv/
ai-worker/venv/
ai-worker/__pycache__/
ai-worker/**/__pycache__/
ai-worker/**/*.pyc
ai-worker/.pytest_cache/
ai-worker/.mypy_cache/
ai-worker/*.egg-info/
# Pytest also runs from the repo root in some flows (e.g. when the
# user picks the workspace as basetemp). The artifacts are pure cache
# / fixture scratch — never committed.
.pytest_cache/
.mypy_cache/
.tmp/
**/__pycache__/
**/*.pyc

# ---- Scratch tests / scripts / harnesses ----
# Forward-looking patterns: anything matching is treated as personal
# scratch and stays out of git. Promote a file to a real name (drop
# the prefix / suffix) when you want it tracked.
**/test_scratch_*.py
**/scratch_*.py
**/_scratch_*.py
**/_tmp_*.py
**/tmp_*.py
**/*_scratch.py
**/*_sandbox.py
ai-worker/scripts/_scratch/
ai-worker/scripts/_drafts/
ai-worker/eval/harness/_scratch/
ai-worker/eval/harness/_drafts/
ai-worker/tests/_scratch/
ai-worker/tests/_drafts/

# ---- Eval harness generated artifacts ----
# Reports change every run and should not be committed. Sample OCR
# fixture images are synthesized by scripts/make_ocr_sample_fixtures.py
# and drift across fonts, so they are regenerated on demand. Phase-0
# baseline reports are the one exception — they anchor the README
# benchmark table and later phases diff against them.
ai-worker/eval/reports/*
!ai-worker/eval/reports/baseline-phase0.json
!ai-worker/eval/reports/baseline-phase0-anime.json
# Phase 8 agent-loop decision: the markdown + backing JSON are the
# record of the measured A/B, not a run-to-run report.
!ai-worker/eval/reports/agent-loop-decision.md
!ai-worker/eval/reports/agent-loop-decision.json
# Phase 2A reranker summary artifacts — small, deterministic, anchor
# the README benchmark table. We commit the flat candidate-recall /
# comparison / failure-analysis files; the per-run retrieval dirs
# (top_k_dump.jsonl, miss_analysis, duplicate_analysis) stay local
# because they're large and reproducible from corpus + dataset.
!ai-worker/eval/reports/phase2a-reranker/
ai-worker/eval/reports/phase2a-reranker/*
!ai-worker/eval/reports/phase2a-reranker/candidate-recall-b2.json
!ai-worker/eval/reports/phase2a-reranker/candidate-recall-b2.md
!ai-worker/eval/reports/phase2a-reranker/reranker-comparison.json
!ai-worker/eval/reports/phase2a-reranker/reranker-comparison.md
!ai-worker/eval/reports/phase2a-reranker/reranker-failure-analysis.json
!ai-worker/eval/reports/phase2a-reranker/reranker-failure-analysis.md
# Locked legacy retrieval baseline — the A/B reference for LangGraph
# adoption. Files are deterministic post-process output of
# scripts/build_legacy_baseline_final.py (no top_k_dump or per-row
# JSON, only the consolidated metrics + manifest + README).
!ai-worker/eval/reports/legacy-baseline-final/
ai-worker/eval/reports/legacy-baseline-final/*
!ai-worker/eval/reports/legacy-baseline-final/README.md
!ai-worker/eval/reports/legacy-baseline-final/metrics.json
!ai-worker/eval/reports/legacy-baseline-final/metrics.md
!ai-worker/eval/reports/legacy-baseline-final/latency_breakdown.json
!ai-worker/eval/reports/legacy-baseline-final/latency_breakdown.md
!ai-worker/eval/reports/legacy-baseline-final/selected_config.json
!ai-worker/eval/reports/legacy-baseline-final/baseline_manifest.json
# Phase 7.0 retrieval-title A/B — commit the small decision artefacts
# (final report, A/B summary, diff report, improved/regressed/per-query
# JSONL, manifest digests, generated silver query set). The two
# rag_chunks_<variant>.jsonl exports (~318 MB each) are
# deterministically reproducible from the Phase 6.3 source via
# eval.harness.v4_chunk_export and stay local.
!ai-worker/eval/reports/namu-v4-structured-combined-2008-2026-04-phase7_0_retrieval_title_ab/
ai-worker/eval/reports/namu-v4-structured-combined-2008-2026-04-phase7_0_retrieval_title_ab/*
!ai-worker/eval/reports/namu-v4-structured-combined-2008-2026-04-phase7_0_retrieval_title_ab/PHASE7_0_FINAL_REPORT.md
!ai-worker/eval/reports/namu-v4-structured-combined-2008-2026-04-phase7_0_retrieval_title_ab/ab_summary.json
!ai-worker/eval/reports/namu-v4-structured-combined-2008-2026-04-phase7_0_retrieval_title_ab/ab_summary.md
!ai-worker/eval/reports/namu-v4-structured-combined-2008-2026-04-phase7_0_retrieval_title_ab/manifest_title_section.json
!ai-worker/eval/reports/namu-v4-structured-combined-2008-2026-04-phase7_0_retrieval_title_ab/manifest_retrieval_title_section.json
!ai-worker/eval/reports/namu-v4-structured-combined-2008-2026-04-phase7_0_retrieval_title_ab/variant_diff_report.json
!ai-worker/eval/reports/namu-v4-structured-combined-2008-2026-04-phase7_0_retrieval_title_ab/variant_diff_report.md
!ai-worker/eval/reports/namu-v4-structured-combined-2008-2026-04-phase7_0_retrieval_title_ab/queries_v4_silver.jsonl
!ai-worker/eval/reports/namu-v4-structured-combined-2008-2026-04-phase7_0_retrieval_title_ab/per_query_comparison.jsonl
!ai-worker/eval/reports/namu-v4-structured-combined-2008-2026-04-phase7_0_retrieval_title_ab/improved_queries.jsonl
!ai-worker/eval/reports/namu-v4-structured-combined-2008-2026-04-phase7_0_retrieval_title_ab/regressed_queries.jsonl
ai-worker/eval/datasets/samples/

# ---- Phase 7.0 dense FAISS indexes (local-only, ~1.3 GB total) ----
# faiss.index + chunks.jsonl + variant_manifest.json are rebuilt from
# the chunk exports via eval.harness.v4_index_builder.build_v4_variant_index.
# Variant manifest digests are recorded in PHASE7_0_FINAL_REPORT.md.
ai-worker/eval/indexes/

# ---- Agent loop A/B harness run output ----
# Each run writes raw_results.jsonl / summary.csv / comparison_summary.json
# under eval/agent_loop_ab/<run-name>/ plus a FAISS index cache under
# _indexes/ and a console log under _logs/. Only the README and the
# tracked smoke fixtures (smoke.csv / smoke.jsonl) are committed.
ai-worker/eval/agent_loop_ab/_indexes/
ai-worker/eval/agent_loop_ab/_logs/
ai-worker/eval/agent_loop_ab/legacy-vs-graph-*/
ai-worker/eval/agent_loop_ab/smoke-*/

# ---- Optuna tuning artifacts ----
# Keep summary.md + config.yaml (deterministic replay), drop the
# binary SQLite study and per-run plots. See docs/tuning.md.
ai-worker/eval/experiments/studies/**/study.db
ai-worker/eval/experiments/studies/**/study.db-journal
ai-worker/eval/experiments/studies/**/plots/

# ---- Demo script generated assets ----
scripts/assets/

# ---- Local runtime state ----
data/
!data/.gitkeep
local-storage/
!local-storage/.gitkeep
*.log

# ---- Docker volumes (if bound locally) ----
infra/postgres/data/
infra/redis/data/
infra/minio/data/

# ---- Phase 9 dataset artifacts (local-only) ----
# The full 1,764-title namu-wiki sample lives outside the repo; if a
# contributor stages it locally, keep it out of git. We commit only the
# 300-title reservoir sample at fixtures/anime_corpus_kr.jsonl (~17 MB).
ai-worker/fixtures/corpus_anime_full/
# sample_check.py HTML reports + reviewer annotation JSONs are local.
review/

# ---- Retrieval-eval corpora (local-only, large) ----
# Full source corpora live under ai-worker/eval/corpora/<name>/corpus.jsonl
# and are too large to commit (e.g. namu_anime_v3 is ~261 MB). The
# accompanying README.md is committed so contributors know how to
# re-stage the file from its source path.
ai-worker/eval/corpora/**/*.jsonl
!ai-worker/eval/corpora/**/README.md
# Generated silver query sets are reproducible via
# eval.harness.generate_eval_queries; keep them local. Hand-authored
# smoke and gold sets ARE committed (small + verified).
ai-worker/eval/eval_queries/*_silver_*.jsonl
ai-worker/eval/eval_queries/*_synthetic_*.jsonl
ai-worker/eval/eval_queries/_generation_log_*.jsonl

# ---- RAG build artifacts (local-only) ----
# FAISS index + build manifest are rebuilt from fixtures via
# scripts/build_rag_index.py; keep them out of git.
rag-data/*.index
rag-data/build.json

# ---- Optuna round-refinement run artifacts ----
# study_bundle.json + llm_input.md emitted by the skill's round_runner
# are per-run artifacts; only the .gitkeep is tracked so the directory
# survives a fresh clone.
ai-worker/eval/experiments/run_output/
!ai-worker/eval/experiments/run_output/.gitkeep

# ---- Working notes / decision logs / personal context ----
# Trade-off records, session work logs, and decision/process docs are
# local-only by default. They typically capture per-author context,
# private discussion, or in-progress reasoning that does not belong on
# the main branch. Promote a doc out of these patterns only when it is
# explicitly meant as canonical project documentation.
**/*-tradeoffs.md
**/*-tradeoff.md
**/*-trade-offs.md
**/*-trade-off.md
**/*-session-log.md
**/*-session-log-*.md
**/*-worklog.md
**/*-worklog-*.md
**/*-personal.md
**/*-private.md
**/notes/
**/_scratch/
**/_local/
**/_drafts/
# Phase 0 baseline tradeoffs / decisions doc — local-only by policy.
ai-worker/eval/reports/phase0-baseline-tradeoffs.md

